{{#if (and siteSettings.wallet_login_enabled (not currentUser))}}
  <div class="custom-sso-login">
    <a
      href="/session/sso?return_path=/"
      class="btn btn-primary btn-block custom-sso-btn"
      rel="nofollow noopener"
    >
      {{d-icon "wallet"}}
      Войти через криптокошелёк
    </a>
  </div>
{{/if}}
